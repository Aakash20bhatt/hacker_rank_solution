if __name__ == '__main__':
    n = int(input())
    arr = list(map(int, input().split()))
    ar=max(arr)
    for i in range(n):
        if ar == max(arr):
            arr.remove(max(arr))
    m=max(arr)
    print(m)
